console.log("hello");
const add = (a, b) => a + b;
const subtract = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b;
// console.log(add(1, 1), subtract(1, 1), multiply(1, 1), divide(1, 1));
export { add, subtract, multiply, divide };
